itle>
<link rel="icon" type="image/png" href="favicon.png">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;0,900;1,700&family=DM+Sans:wght@300;400;500;600&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
*, *::before, *::after { margin:0; padding:0; box-sizing:border-box; }
:root {
  --cream:  #f5f5f5;
  --red:    #f05454;
  --steel:  #30475e;
  --ink:    #121212;
  --sidebar-w: 64px;
  --nav-h: 60px;
  --red-dim: rgba(240,84,84,0.12);
  --steel-dim: rgba(48,71,94,0.15);
}
html { scroll-behavior:smooth; }
body {
  font-family:'DM Sans',sans-serif;
  background: var(--ink);
  color: var(--cream);
  min-height:100vh;
  overflow-x:hidden;
}

/* ─── SCROLLBAR ─── */
::-webkit-scrollbar { width:5px; }
::-webkit-scrollbar-track { background:var(--ink); }
::-webkit-scrollbar-thumb { background:var(--steel); border-radius:10px; }

/* ─── NAVBAR ─── */
nav {
  position:fixed; top:0; left:0; right:0; height:var(--nav-h);
  background: rgba(18,18,18,0.92);
  backdrop-filter: blur(16px);
  border-bottom: 1px solid rgba(240,84,84,0.15);
  display:flex; align-items:center; justify-content:space-between;
  padding: 0 28px;
  z-index:1000;
}
.logo {
  font-family:'Playfair Display',serif;
  font-size:1.4rem; font-weight:900;
  color:var(--cream);
  letter-spacing:-0.02em;
  display:flex; align-items:center; gap:8px;
}
.logo-dot { width:8px; height:8px; border-radius:50%; background:var(--red); animation:blink 2s ease-in-out infinite; }
@keyframes blink { 0%,100%{opacity:1;} 50%{opacity:0.3;} }

.profile-wrap { position:relative; }
.profile-pill {
  display:flex; align-items:center; gap:10px;
  background: rgba(48,71,94,0.3);
  border:1px solid rgba(48,71,94,0.5);
  border-radius:40px; padding:6px 14px 6px 8px;
  cursor:pointer; transition:all 0.25s;
}
.profile-pill:hover { background:rgba(48,71,94,0.5); border-color:var(--red); }
.avatar {
  width:32px; height:32px; border-radius:50%;
  background:linear-gradient(135deg, var(--red), var(--steel));
  display:flex; align-items:center; justify-content:center;
  font-size:0.85rem; font-weight:700; color:white;
  flex-shrink:0;
}
.profile-name { font-size:0.83rem; font-weight:600; color:var(--cream); }
.profile-rank { font-size:0.68rem; color:rgba(245,245,245,0.4); font-family:'DM Mono',monospace; }
.profile-chevron { font-size:0.7rem; color:rg